---
import ConsoleLayout from '@/layouts/ConsoleLayout.astro';
import { withBase } from '@/lib/utils';
---

<ConsoleLayout title="404 - Not Found" description="Page not found">
  <div class="error-content">
    <pre class="ascii-error" aria-hidden="true">
{`
██╗  ██╗ ██████╗ ██╗  ██╗
██║  ██║██╔═████╗██║  ██║
███████║██║██╔██║███████║
╚════██║████╔╝██║╚════██║
     ██║╚██████╔╝     ██║
     ╚═╝ ╚═════╝      ╚═╝
`}
    </pre>

    <h1>Page Not Found</h1>
    <p class="error-message">The requested path does not exist.</p>

    <div class="error-suggestions">
      <p>Try one of these:</p>
      <ul>
        <li><code>cd /</code> - <a href={withBase("/")}>Go home</a></li>
        <li><code>cd /blog</code> - <a href={withBase("/blog")}>View blog</a></li>
        <li><code>cd /projects</code> - <a href={withBase("/projects")}>View projects</a></li>
        <li><code>cd /about</code> - <a href={withBase("/about")}>About me</a></li>
      </ul>
    </div>
  </div>
</ConsoleLayout>

<style>
  .error-content {
    text-align: center;
    padding: var(--space-lg);
  }

  .ascii-error {
    font-size: clamp(0.4rem, 2vw, 0.8rem);
    line-height: 1.1;
    color: var(--error);
    margin-bottom: var(--space-lg);
  }

  h1 {
    color: var(--error);
    margin-bottom: var(--space-sm);
  }

  .error-message {
    color: var(--text-muted);
    margin-bottom: var(--space-lg);
  }

  .error-suggestions {
    text-align: left;
    max-width: 300px;
    margin: 0 auto;
  }

  .error-suggestions p {
    color: var(--text-secondary);
    margin-bottom: var(--space-sm);
  }

  .error-suggestions ul {
    list-style: none;
    padding: 0;
  }

  .error-suggestions li {
    padding: var(--space-xs) 0;
  }

  .error-suggestions code {
    color: var(--accent);
    background: none;
    padding: 0;
  }
</style>
